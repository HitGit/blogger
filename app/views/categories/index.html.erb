<h1>Categories</h1>

<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.name, category %>&nbsp; <%= button_to 'X', category, method: :delete, data: { confirm: 'Are you sure?' } %> </li>
  <% end %>
</ul>


<%= button_to 'New category', '/categories/new', method: :get %>